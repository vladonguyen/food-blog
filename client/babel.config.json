{
    "presets": [
        "@babel/preset-env",
        [
            "@babel/preset-react",
    {
        "runtime": "automatic"
    }
],
        "babel-preset-vite" // this line made a miracle so jest works with import.meta.env.VITE_API_URL
    ]
}